name: iOS Build

on:
  push:
    branches:
      - main
      - wiki-source-picker-ci
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build Wikipedia iOS
    runs-on: macos-15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode_16.4.app || sudo xcode-select -s /Applications/Xcode.app
          xcodebuild -version

      - name: Generate open source build configuration
        run: |
          bash scripts/setup_bundle_id ci
          test -f Configurations/OpenSourceDebug.xcconfig

      - name: Show available schemes
        run: |
          xcodebuild -list -project Wikipedia.xcodeproj

      - name: Build app for simulator
        run: |
          set -o pipefail
          xcodebuild \
            -project Wikipedia.xcodeproj \
            -scheme Wikipedia \
            -destination 'generic/platform=iOS Simulator' \
            -configuration Debug \
            ONLY_ACTIVE_ARCH=YES \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            build 2>&1 | tee xcodebuild.log

      - name: Print build errors
        if: failure()
        run: |
          echo '---- xcodebuild errors ----'
          grep -n -i -E '(^|[^A-Za-z])(error:|fatal error:|Command .* failed|BUILD FAILED)' xcodebuild.log | tail -80 || true
